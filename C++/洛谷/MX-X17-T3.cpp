#include <bits/stdc++.h>

using namespace std;

namespace RealDream {
	typedef long long ll;
	int main() {
		/**
		 * RealDream AK IOI
		 */
		ios::sync_with_stdio(false);
		cin.tie(nullptr);
		cout.tie(nullptr);
		int t;
		cin >> t;
		while (t--) {
			int n;
			cin >> n;
			for (int i = 0; i < n; i++) {
				int x;
				cin >> x;
			}
			if (n % 2 == 0) cout << n << endl;
			else cout << n - 1 << endl;
		}
		return 0;
	}
};

int main() {
	/**
	 * Made By RealDream
	 */
	RealDream::main();
	return 0;
}
